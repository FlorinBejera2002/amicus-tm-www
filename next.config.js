/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['164.90.166.249'],
    formats: ['image/avif', 'image/webp'],
  },
}

module.exports = nextConfig
